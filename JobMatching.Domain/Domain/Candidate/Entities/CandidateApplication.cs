namespace JobMatching.Domain.Domain.Candidate.Entities
{
    public class CandidateApplication
    {
        //public Guid JobId { get; private set; }
        //public string JobTitle { get; private set; } = string.Empty;
        //public ApplicationStatus Status { get; private set; }
        //public DateTime ApplicationDate { get; private set; }

        //private CandidateApplication(
        //    Guid jobId, string jobTitle, ApplicationStatus status, DateTime applicationDate)
        //{
        //    JobId = jobId;
        //    JobTitle = jobTitle;
        //    Status = status;
        //    ApplicationDate = applicationDate;
        //}

        //public static Result<CandidateApplication> Create(
        //    Guid jobId, string jobTitle, ApplicationStatus status, DateTime applicationDate)
        //{
        //    if (jobId == Guid.Empty) return Result.
        //}
    }
}
